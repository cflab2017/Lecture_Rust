// 핵심 포인트:
// - println! 의 `{}` 위치 인수는 뒤에 나열한 값과 1:1 로 대응된다.
// - 표현식 `a + b`, `a * b` 의 결과가 그대로 인수로 전달된다.

fn main() {
    let a = 7;
    let b = 3;

    println!("{} + {} = {}", a, b, a + b);
    println!("{} * {} = {}", a, b, a * b);
}
